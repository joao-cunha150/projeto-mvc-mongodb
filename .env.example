# Copie este arquivo para ".env" e preencha com seus dados reais.
# NUNCA suba o arquivo ".env" de verdade para o GitHub.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=mysql
DB_NAME=loja

PORT=3000

# Chave usada para assinar os tokens JWT (troque por qualquer texto seu)
JWT_SECRET=troque_esta_chave_por_um_texto_secreto_seu
